[project]
name = "life-tracker-tooling"
version = "0.1.0"
description = "Development tooling for the life-tracker Rust project"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4,<5",
]

[tool.uv]
package = false
